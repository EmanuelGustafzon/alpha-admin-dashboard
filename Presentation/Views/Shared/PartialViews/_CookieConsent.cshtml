<div id="cookie-consent" class="form-modal d-none cookie-consent-container">
    <div class="flex mb-4">
        <h6>You are in control of your data</h6>
    </div>
    <div class="mb-4">
        <p>We store your personal preferences, like your mode while using this page</p>
        <p>This is to enhance the experience of our users.</p>
    </div>
    <form id="cookie-consent-form" class="cookie-consent-form">
        <div class="form-group flex space-between">
            <p>Essential (always active)</p>
            <label class="toggle-container">
                <input id="cookie-essential" checked disabled class="form-control" type="checkbox" name="essential">
                <span class="toggle-slider"></span>
            </label>
        </div>
        <div class="form-group flex space-between">
            <p>Functional</p>
            <label class="toggle-container">
                <input id="cookie-functional" type="checkbox" name="functional">
                <span class="toggle-slider"></span>
            </label>
        </div>
        <div class="d-grid">
            <button onclick="acceptAllCookies()" class="btn btn-primary">Accept All</button>
        </div>
        <div class="d-grid">
            <button onclick="acceptSelectedCookies()" class="btn btn-secondary">Accept Selected Settings</button>
        </div>
    </form>
</div>